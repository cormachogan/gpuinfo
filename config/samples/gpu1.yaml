apiVersion: topology.corinternal.com/v1
kind: GPUInfo
metadata:
  name: gpu1
spec:
  # Desired Accelerator Time (desAccTime) should be specified in hours
  # GPU Required should be True or False
  desAccTime: 100
  gpuRequired: True
